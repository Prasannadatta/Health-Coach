## 📂 Project Structure

```text
.
├── data/              # JSON/JSONL Q&A files, CSVs, etc. (usually not all pushed)
├── models/            # Training / inference scripts (e.g. Lora_FineTune.py)
├── notebooks/         # Jupyter notebooks for exploration & data generation
├── outputs/           # Checkpoints, LoRA adapters, logs (ignored in git)
├── LICENSE
├── README.md
├── requirements.txt   # Python dependencies
└── .gitignore         # Files/folders to exclude from git